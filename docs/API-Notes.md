# API Notes

## MBTA V3 API

Browser-direct API calls are used for alpha/Phase 1. This keeps the setup simple and requires no backend.

**TODO (Phase 10):** Move MBTA API calls to a Vercel serverless proxy and store the API key in a Vercel environment variable so the key is not exposed in client-side JavaScript.
